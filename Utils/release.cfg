PACKAGE=locarna

TEST=false ## whether to do test/dry run; only change anything, if TEST=false
LOCAL=true ## install to local web home only; no hg/cvs-access

CHECK=false ## run dist check only if CHECK=true (otherwise run dist-gzip only)
CHECK_CLANG=true ## if true (and CHECK=true), rerun check with clang 

DOXYGEN=false # if true, generate and install doxygen documentation

LOCARNA_HOME=`pwd`
BUILDDIR="$LOCARNA_HOME/_build"

WEB_LOCARNA_HOME=Software/LocARNA
WEBWORKDIR=$HOME/public_html/web/$WEB_LOCARNA_HOME
WEBSERVER=bicvs.informatik.uni-freiburg.de
WEBSERVER_BASEDIR=/space/web
